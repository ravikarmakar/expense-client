/**
 * Initial index route entry point.
 * Centralized authentication gating and splash overlay are handled
 * by <AuthGuard /> in app/_layout.tsx to prevent duplicate redirects
 * and double rendering on app startup.
 */
export default function IndexScreen() {
  return null;
}
