const reactNativeConfig = require('@workspace/eslint-config/react-native.js');

module.exports = [
  ...reactNativeConfig,
  {
    rules: {
      // App-specific rules can be added here
    },
  },
];
